SET(MUMPS_LIB_MPISEQ_SOURCES
elapse.c
mpic.c
mpi.f
)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})


IF(ENABLE_SHARED)
  ADD_LIBRARY(mpiseq SHARED ${MUMPS_LIB_MPISEQ_SOURCES})
  #SET_TARGET_PROPERTIES(mpiseq PROPERTIES LINK_FLAGS '-Wl,-z,defs')
ELSE(ENABLE_SHARED)
  ADD_LIBRARY(mpiseq STATIC ${MUMPS_LIB_MPISEQ_SOURCES})
ENDIF(ENABLE_SHARED)
INSTALL(TARGETS mpiseq DESTINATION lib)

